// This template needs to specify properties (on the left) and their type(?) on the right
export interface StudentInterface {
    ranking: number;
    name: string;
    age: number;
    feedback: string;
}